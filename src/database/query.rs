pub fn list_fields() -> Vec<String> {
    let available_fields = Vec::new();
    return available_fields;
}

pub fn list_titles() -> Vec<String> {
    // TODO this should be a `title' type
    let available_titles = Vec::new();
    return available_titles;
}